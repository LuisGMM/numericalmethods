[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = '--cov=charliepy'
testpaths = [
    'tests',
]

[tool.mypy]
mypy_path = 'src'
check_untyped_defs = true
disallow_any_generics = true
